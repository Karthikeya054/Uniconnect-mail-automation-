{
    "name": "uniconnect-mail-automation",
    "version": "1.3.0",
    "private": true,
    "packageManager": "pnpm@10.26.2",
    "scripts": {
        "dev": "npx pnpm -r --parallel run dev",
        "build": "npx pnpm -r run build",
        "test": "npx pnpm -r run test",
        "seed": "npx pnpm -F @uniconnect/shared run seed"
    },
    "typecheck": "npx pnpm -r run check",
    "devDependencies": {
        "typescript": "^5.0.0",
        "eslint": "^9.0.0",
        "prettier": "^3.0.0",
        "eslint-config-prettier": "^9.0.0",
        "uuid": "^10.0.0"
    },
    "engines": {
        "node": ">=18"
    },
    "pnpm": {
        "onlyBuiltDependencies": [
            "esbuild"
        ]
    }
}